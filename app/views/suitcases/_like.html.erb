<%= form_for(current_user.likes.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :suitcase_id, @suitcase.id %></div>
  <%= f.submit "お気に入り", class: "btn" %>

<% end %>

<!--i class="fa fa-star-o"></i-->